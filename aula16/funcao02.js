function soma(n1 = 0, n2 = 0) {
    return n1 + n2
}

console.log(soma(2))
// soma -> chamada
// os parâmetros 2 e 5 vão para n1 e n2, e é retornado a soma entre os dois.
// parâmetros predefinidos (opcionais)
